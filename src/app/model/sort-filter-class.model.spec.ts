import { SortFilterClass } from '../model/sort-filter-class.model';

describe('SortFilterClass', () => {
  it('should create an instance', () => {
    expect(new SortFilterClass()).toBeTruthy();
  });
});
